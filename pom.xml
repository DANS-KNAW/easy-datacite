<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<parent>
		<artifactId>easy</artifactId>
		<groupId>nl.knaw.dans.easy</groupId>
		<version>2.0-SNAPSHOT</version>
	</parent>

	<modelVersion>4.0.0</modelVersion>
	<name>Easy II</name>
	<groupId>nl.knaw.dans.easy</groupId>
	<artifactId>easy-webui</artifactId>
	<packaging>war</packaging>
	<version>2.0-beta-40</version>
	<description>Easy II web application</description>

	<!--
		TODO <organization> <name>company name</name> <url>company url</url>
		</organization>
	-->

	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<dependencies>
		<!-- Project dependencies -->
		<dependency>
			<groupId>nl.knaw.dans.easy</groupId>
			<artifactId>easy-business</artifactId>
			<version>2.0-SNAPSHOT</version>
		</dependency>

		<dependency>
			<groupId>nl.knaw.dans.common</groupId>
			<artifactId>dans-wicket</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>

		<!-- Dependencies on Repositories and other Data Access Points -->
		<!--
			REMINDER: these dependencies are only here so that Spring can
			instantiate and inject these dependencies!
		-->
		<!--
			There should never be a dependency on these projects from the code
			body!
		-->
		<!--
			TEST: comment out these dependencies and the project should still
			compile.
		-->
		<!-- (It would be better if we could do this in a different way) -->
		<dependency>
			<groupId>nl.knaw.dans.easy</groupId>
			<artifactId>easy-ldap</artifactId>
			<version>2.0-SNAPSHOT</version>
		</dependency>

		<dependency>
			<groupId>nl.knaw.dans.easy</groupId>
			<artifactId>easy-fedora</artifactId>
			<version>2.0-SNAPSHOT</version>
		</dependency>

		<dependency>
			<groupId>nl.knaw.dans.common</groupId>
			<artifactId>dans-solr</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>

		<!-- End dependencies on Repositories and other Data Access Points -->

		<!-- End project dependencies -->

		<!--  WICKET DEPENDENCIES --> 
		<dependency>
			<groupId>org.wicketstuff</groupId>
			<artifactId>wicket-contrib-jamon</artifactId>
			<version>1.3-SNAPSHOT</version>
		</dependency>
		
		<!-- Spring dependencies 
		<dependency>
			<groupId>aspectj</groupId>
			<artifactId>aspectjweaver</artifactId>
			<version>1.5.4</version>
		</dependency>
		-->
		<!-- ASPECTJ -->
		<dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjrt</artifactId>
            <version>1.6.10</version>
        </dependency>


		<!--  JETTY DEPENDENCIES FOR TESTING  -->
		<dependency>
			<groupId>org.mortbay.jetty</groupId>
			<artifactId>jetty</artifactId>
			<version>${jetty.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.mortbay.jetty</groupId>
			<artifactId>jetty-util</artifactId>
			<version>${jetty.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.mortbay.jetty</groupId>
			<artifactId>jetty-management</artifactId>
			<version>${jetty.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.json</groupId>
			<artifactId>json</artifactId>
			<version>20080701</version>
		</dependency>
		<dependency>
			<groupId>commons-collections</groupId>
			<artifactId>commons-collections</artifactId>
			<version>3.2.1</version>
		</dependency> 
		
		<dependency>
			<groupId>org.powermock</groupId>
			<artifactId>powermock-api-easymock</artifactId>
			<version>1.4.6</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.powermock</groupId>
			<artifactId>powermock-module-junit4</artifactId>
			<version>1.4.6</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.easymock</groupId>
			<artifactId>easymock</artifactId>
			<version>3.0</version>
			<scope>test</scope>
		</dependency>
		

	</dependencies>

	<build>
		<!-- RSK 2009-04-03: Disabled. Build is reflected in application -->
		<!--
			finalName>${project.artifactId}-${project.version}-b${buildNumber}</finalName
		-->
		<!--
			finalName>easy-${project.version}</finalName
		-->
		<finalName>easy</finalName>

		<!--
			RSK 2009-04-03: Disabled, entries disable buildprocess. Fails on
			tests. Most probable cause of failure is the fact that if we want to
			add resource-directives (for filtering), we need to add an entry for
			every location resources are used. Testresources should be located in
			/src/test/*, but apparently the testcode needs access to other
			locations as well. As long as I don't know where to look, we can't
			use filtering <sourceDirectory>src/main/java</sourceDirectory>
			<testSourceDirectory>src/test/java</testSourceDirectory> <resources>
			<resource> <directory>src</directory> <filtering>true</filtering>
			<includes> <include>**/*.*</include> </includes> </resource>
			</resources> <testResources> <testResource>
			<directory>src</directory> <includes> <include>**/*.*</include>
			</includes> </testResource> </testResources>
		-->
		
		<!--  
		<sourceDirectory>src/main/java</sourceDirectory>
        <testSourceDirectory>src/test/java</testSourceDirectory>
        <resources>
            <resource>
                <filtering>true</filtering>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*.*</include>
                </includes>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
            
            <resource>
                <filtering>true</filtering>
                <directory>src/main/resources</directory>
                <includes>
                    <include>**/*.*</include>
                </includes>
            </resource>
        </resources>
        <testResources>
            <testResource>
                <filtering>true</filtering>
                <directory>src/test/resources</directory>
                <includes>
                    <include>**/*.*</include>
                </includes>
            </testResource>
        </testResources>
        
        -->

		<plugins>
			<plugin>
				<groupId>org.mortbay.jetty</groupId>
				<artifactId>maven-jetty-plugin</artifactId>
				<version>${jetty.version}</version>
			</plugin>

			<!--
				<plugin> <groupId>org.codehaus.mojo</groupId>
				<artifactId>buildnumber-maven-plugin</artifactId> <executions>
				<execution> <phase>validate</phase> <goals> <goal>create</goal>
				</goals> </execution> </executions> <configuration>
				<doCheck>false</doCheck> <doUpdate>false</doUpdate> </configuration>
				</plugin>
			-->
        <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <groupId>org.apache.maven.plugins</groupId>
                <configuration>
                    <source>${source-code-version}</source>
                    <target>${byte-code-version}</target>
                    <debug>true</debug>
                </configuration>
            </plugin>
            
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <useFile>true</useFile>
                    <systemProperties>
                        <property>
                            <name>net.sourceforge.cobertura.datafile</name>
                            <value>${basedir}/target/cobertura/cobertura.ser</value>
                        </property>
                    </systemProperties>
                    <excludes>
                        <exclude>**/*OnlineTest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
            
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <configuration>
                    <enableRulesSummary>false</enableRulesSummary>
                </configuration>
            </plugin>
            
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>aspectj-maven-plugin</artifactId>
                <version>1.3.1</version>
                <configuration>
                	<verbose>true</verbose>
                    <source>${source-code-version}</source>
                    <target>${byte-code-version}</target>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                            <!--  goal>test-compile</goal -->
                        </goals>
                    </execution>
               </executions>

           </plugin>

		</plugins>
	</build>

	<properties>
		<!-- wicket.version>1.3.4</wicket.version -->
		<jetty.version>6.1.7</jetty.version>
	</properties>
</project>

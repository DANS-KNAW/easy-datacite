<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>nl.knaw.dans</groupId>
	<artifactId>easy-sword</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>easy sword</name>
	<packaging>war</packaging>

	<organization>
		<name>dans</name>
		<url>http://www.dans.knaw.nl</url>
	</organization>
	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>
	
	<!-- Note: the latest nl.knaw.dans.easy.version is >>NOT<< in artifactory. -->
	<!-- So you can only build this project when the nl.knaw.dans.easy.version -->
	<!-- (see below) is in your editor or in your .m2 repository.              -->
	
	<properties>
		<source-code-version>1.6</source-code-version>
		<byte-code-version>1.6</byte-code-version>
		<sword-common.version>1.1</sword-common.version>
		<nl.knaw.dans.easy.version>2.x-SNAPSHOT</nl.knaw.dans.easy.version>
		<junit.version>4.7</junit.version>
		<easymock.version>3.1</easymock.version>
		<jetty.version>6.1.21</jetty.version>
		<nl.knaw.dans.platform.language.version>def-1.0-SNAPSHOT</nl.knaw.dans.platform.language.version>
	</properties>

	<dependencies>
	<!-- 
		<dependency>
			<groupId>nl.knaw.dans.platform.language</groupId>
			<artifactId>def</artifactId>
			<version>${nl.knaw.dans.platform.language.version}</version>
		</dependency>
	 -->
		<dependency>
			<groupId>nl.knaw.dans.easy</groupId>
			<artifactId>easy-fedora</artifactId>
			<version>${nl.knaw.dans.easy.version}</version>
		</dependency>
		<dependency>
			<groupId>nl.knaw.dans.easy</groupId>
			<artifactId>easy-business</artifactId>
			<version>${nl.knaw.dans.easy.version}</version>
		</dependency>
		<dependency>
			<groupId>nl.knaw.dans.easy</groupId>
			<artifactId>easy-ldap</artifactId>
			<version>${nl.knaw.dans.easy.version}</version>
		</dependency>
		<dependency>
			<groupId>sword</groupId>
			<artifactId>sword-common</artifactId>
			<version>${sword-common.version}</version>
		</dependency>
		
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.easymock</groupId>
			<artifactId>easymock</artifactId>
			<version>${easymock.version}</version>
			<!-- not just for testing, the NoOp option is implemented with mocks -->
		</dependency>
		<dependency>
			<groupId>org.mortbay.jetty</groupId>
			<artifactId>jetty</artifactId>
			<version>${jetty.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>
	
	<build>
		<finalName>easy-sword</finalName>
		
		<sourceDirectory>src/main/java</sourceDirectory>
		<testSourceDirectory>src/test/java</testSourceDirectory>
		<testResources>
			<testResource>
				<filtering>true</filtering>
				<directory>src/test/resources</directory>
				<includes>
					<include>**/*.*</include>
				</includes>
				<excludes>
					<exclude>**/*.doc</exclude>
					<exclude>**/*.pdf</exclude>
				</excludes>
			</testResource>
			<testResource>
				<filtering>false</filtering>
				<directory>src/test/resources</directory>
				<includes>
					<include>**/*.doc</include>
					<include>**/*.pdf</include>
				</includes>
			</testResource>
		</testResources>
		
		<plugins>
		
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<groupId>org.apache.maven.plugins</groupId>
				<version>2.3.2</version>
				<configuration>
					<source>${source-code-version}</source>
					<target>${byte-code-version}</target>
					<debug>true</debug>
				</configuration>
			</plugin>
			
			<plugin>
				<artifactId>maven-war-plugin</artifactId>
				<version>2.1.1</version>
				<configuration>
					<!-- provided by tomcat -->
					<packagingExcludes>WEB-INF/lib/servlet-api*.jar</packagingExcludes>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
							<classpathPrefix>lib/</classpathPrefix>
						</manifest>
					</archive>
				</configuration>
			</plugin>
			
			<!-- plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<configuration>
					<enableRulesSummary>false</enableRulesSummary>
				</configuration>
			</plugin-->
			
		</plugins>
	</build>
	
	<repositories>
		<repository>
			<id>central</id>
			<url>http://artifactory.dans.knaw.nl/repo</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
		<repository>
			<id>snapshots</id>
			<url>http://artifactory.dans.knaw.nl/repo</url>
			<releases>
				<enabled>true</enabled>
			</releases>
		</repository>

	</repositories>
	
</project>
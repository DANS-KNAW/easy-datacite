<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="3 minutes">
	<property name="LOGDIR" value="/var/log/easy-proai" />

	<appender name="EASY_PROAI_LOG"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOGDIR}/easy-proai.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- daily rollover -->
			<fileNamePattern>${LOGDIR}/easy-proai.%d{yyyy-MM-dd}.log
			</fileNamePattern>

			<!-- keep 30 days' worth of history -->
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>%p %d [%t] - %m%n</pattern>
		</encoder>
	</appender>

	<appender name="EASY_PROAI_ERROR_LOG"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>ERROR</level>
		</filter>
		<file>${LOGDIR}/easy-proai-error.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- daily rollover -->
			<fileNamePattern>${LOGDIR}/easy-proai-error.%d{yyyy-MM-dd}.log
			</fileNamePattern>

			<!-- keep 30 days' worth of history -->
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>%p %d [%t] - %m%n</pattern>
		</encoder>
	</appender>

	<root level="warn">
		<appender-ref ref="EASY_PROAI_LOG" />
		<appender-ref ref="EASY_PROAI_ERROR_LOG" />
	</root>

	<logger name="proai" level="info" />
	<logger name="fedora" level="info" />
	<logger name="nl.knaw.dans" level="info" />
</configuration>
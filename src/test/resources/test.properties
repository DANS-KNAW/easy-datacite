# properties for nl.knaw.dans.common.lang.test.Tester
tester.test=testing nl.knaw.dans.easy.fedora
tester.verbose=false

# properties used by FedoraClient
fedora.base.url=http://${evm.host.address}:8080/fedora
fedora.admin.username=fedoraAdmin
fedora.admin.userpass=fedoraAdmin

fedora.ri.url=http://${evm.host.address}:8080/fedora/risearch

# Easy FileStore database
#fedora.db.username=easyadmin
#fedora.db.password=easypass
#fedora.db.connectionUrl=jdbc:postgresql://${evm.host.address}:5432/easydb

# Fedora database
fedora.db.username=postgres
fedora.db.password=postgres
fedora.db.connectionUrl=jdbc:postgresql://${evm.host.address}:5432/fedora32

fedora.db.hbnDriverClass=org.postgresql.Driver
fedora.db.hbnDialect=org.hibernate.dialect.PostgreSQLDialect

# filestore-test database
filestore.test.db.username=tester
filestore.test.db.password=tester
filestore.test.db.connectionUrl=jdbc:postgresql://${evm.host.address}:5432/filestore-testdb
filestore.test.db.hbnDriverClass=org.postgresql.Driver
filestore.test.db.hbnDialect=org.hibernate.dialect.PostgreSQLDialect

# Solr
solr.url=http://${evm.host.address}:8080/solr